// Names: Store the names of a few of your friends in a array called
// names. Print each person’s name by accessing each element in the list,
// one at a time.

let friendsName: string[] = ["Sulman","Raza","Talha","Taha"];


for (let a = 0; a < friendsName.length; a++) {
    console.log(friendsName[a]);
}
